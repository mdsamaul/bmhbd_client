import React from 'react';
import { Outlet } from 'react-router-dom';



const Registration = () => {
    return (
        <div>
            <Outlet></Outlet>
        </div>
    );
};

export default Registration;